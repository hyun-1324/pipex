# pipex

pipex is a small program that reproduces the behavior of a UNIX shell pipeline for two commands.
It takes input from a file, passes it throught the first command, pipes the output to a second command, and writes the final result to an output file.
